extends: default

ignore:
  - /frontier/swagger/v1/swagger.yaml
  - /frontier/config/database.yml

rules:
  line-length:
    max: 120
    ignore: |
      frontier/config/locales
  document-start: disable
